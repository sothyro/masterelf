import 'package:flutter/material.dart';

class DecoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ដេគ័រ ស្រុក'),
      ),
    );
  }
}
